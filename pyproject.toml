[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/secplugin"]

[project]
name = "secplugin"
description = "Reverse WebSocket Plugin Framework for Secluded."
version = "1.3.1"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "SumaRoder", email = "1493813167@qq.com"},
]
keywords = ["websocket", "plugin", "secluded", "sdk", "framework"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
]
dependencies = [
    "websockets",
    "typing_extensions",
    "colorlog",
    "watchdog",
]

[project.urls]
Homepage = "https://github.com/SumaRoder/SecPluginPySdk"
Repository = "https://github.com/SumaRoder/SecPluginPySdk"
Issues = "https://github.com/SumaRoder/SecPluginPySdk/issues"

[dependency-groups]
dev = [
    "black>=25.11.0",
    "pyright>=1.1.410",
    "pytest>=8.4.2",
]
